### 3D Game Skeleton

Commands:

Client
```
cd client
npm i
npm run dev
```

Server
```
cd server
npm i
npm run start
```

Open the client dev server URL that Vite prints (default `http://localhost:5173`).


